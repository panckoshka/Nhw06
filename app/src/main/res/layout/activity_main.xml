<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.MainActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/barrier_bottom"
        tools:listitem="@layout/card_post" />

    <androidx.constraintlayout.widget.Barrier
        android:id="@+id/barrier_bottom"
        app:constraint_referenced_ids="undo_changes,undo_changes_content"
        app:barrierDirection="top"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <EditText
        android:id="@+id/content"
        android:inputType="textMultiLine"
        android:background="@android:color/transparent"
        android:hint="@string/post_input_text"
        android:importantForAutofill="no"
        android:padding="@dimen/common_spacing"
        app:layout_constraintEnd_toStartOf="@+id/save"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="0dp"
        android:layout_height="wrap_content" />

    //android:inputType="textMultiLine" - ввод многострочного текста
    //android:background="@android:color/transparent" - убрать линию под текстом
    //android:importantForAutofill="no" - убрать всплывающие подсказки

    <ImageView
        android:id="@+id/save"
        app:layout_constraintTop_toTopOf="@id/content"
        app:srcCompat ="@drawable/ic_baseline_send_24"
        android:padding="@dimen/iv_save_padding"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <androidx.constraintlayout.widget.Group
        android:id="@+id/group_undo_changes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="visible"
        app:constraint_referenced_ids="undo_changes_content,undo_changes" />

    <ImageView
        android:id="@+id/undo_changes"
        android:src="@drawable/undo_changes"
        android:padding="@dimen/iv_save_padding"
        app:layout_constraintTop_toTopOf="@+id/undo_changes_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/undo_changes_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/undo_changes_content"
        android:background="@android:color/transparent"
        android:singleLine="true"
        android:ellipsize="end"
        android:hint="@string/post_input_text"
        android:importantForAutofill="no"
        android:padding="@dimen/common_spacing"
        app:layout_constraintEnd_toStartOf="@+id/undo_changes"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toTopOf="@id/content"
        android:layout_width="0dp"
        android:layout_height="wrap_content" />

</androidx.constraintlayout.widget.ConstraintLayout>

